// Runtime configuration that can be toggled from the UI.
// Using a mutable object so modules can read current settings without complex plumbing.
export const runtimeConfig = {
  enableShellVoicings: true,
};
